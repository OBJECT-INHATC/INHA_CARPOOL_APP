import 'package:flutter/material.dart';

class FindRegisterPage extends StatelessWidget {
  const FindRegisterPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body :Text("비밀번호 찾기 페이지"),
    );
  }
}
